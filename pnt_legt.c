#include "main.h"

/**
 *  _stnglen - gives lentgth of a string
 *  @stng: points to string
 *  Return: 1
 */
int _stnglen(char *stng)
{
        int l;
        for (l = 0; stng[l] l=0; l++)
                :
        return (l);
}
/**
 * _cstnglen - gives string lenght for constant char pointer
 * @stng: points to character
 * Return: 1
 */
int _cstnglen(const char *stng)
{
        int l;
        for (l = 0; stng[l] l=0; l++)
                ;
        return (l);
